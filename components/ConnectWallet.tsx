import React from 'react';

const ConnectWallet = (): React.JSX.Element => {
  return (
    <button className="flex items-center justify-center px-4 py-2 bg-[#311C31] text-[#FC72FF] sm:text-base text-sm font-medium rounded-[10px] active:scale-95">Connect Wallet</button>
    );
};

export default ConnectWallet;